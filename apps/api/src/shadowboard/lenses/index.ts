// Lens registry for the UI. The actual lens execution is data-driven in
// ../board.ts using LENS_PROMPTS; this re-exports descriptive metadata.
export { LENSES, LENS_KEYS } from "@erebus/core";
export type { LensKey } from "@erebus/core";
